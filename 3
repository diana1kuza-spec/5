USD_TO_RUB = 95.50

def convert_usd_to_rub(amount_usd): 1 usage
      """

      Конвертирует сумму из долларов в рубли по текущему курсу.

      Args:

           amount_usd (float): Сумма в долларах

      Returns:

          float: Сумма в рублях

      """

return amount_usd USD_TO_RUB

amount = float(input("Введите сумму в долларах: "))

result = convert_usd_to_rub(amount)

print(f*{amount} USD = {result:.2f} RUB")
